> **Doc:** docs/setup/FE_MATCH_BE_TIP_CONTINUE_PROMPT.txt
> **Updated:** 2026-09-09 20:30 IST
> **Session:** Docs sync — SUPER_ADMIN + returnTripLogId/tripLeg + SQLite v3
================================================================================
COPY FROM HERE → paste as new chat START (FE match BE tip)
================================================================================

@PROJECT_BRAIN.md
@PROMPT_SCOPE.md
@CHAT_PROMPTS.txt
@docs/setup/FE_MATCH_BE_TIP_CONTINUE_PROMPT.txt
@docs/setup/DISCUSSION_STATUS.md
@docs/setup/BRANCH_HOLD_NOTES.md
@docs/API_CONTRACTS.md
@docs/setup/JWT_LOGIN_IMPLEMENTATION_NOTES.txt
@docs/setup/ADMIN_BOOTSTRAP_DRAFT.md
@docs/setup/LOGIN_JSON_FIELDS.txt
@docs/setup/SQLITE_TABLES_COLUMNS.txt
@docs/setup/SCHEMA_FINAL_DRAFT.txt
@docs/setup/RETURN_TRIP_API_GAP.md
@docs/setup/RETURN_QR_UI_PREP.md
@docs/ROUTING_AND_AUTH.md
@lib/features/batches/README.md
@lib/features/d2d/README.md
@docs/LOCAL_DEV.md
@docs/TESTING.md

Task: **Frontend match pass** — verify `professor-cts` against backend tip `professor-dock`,
list gaps, then fix FE (or flag true BE gaps) so schema/API/UI stay aligned.

REPOS:
  FE  D:\cts_beta     work on **professor-cts** only (day lane)
  BE  D:\cts-docker   read/compare **professor-dock** (do not change BE unless user asks)
  Never day-work or push `main`. Do not FF gb-* / merger lanes unless user asks.

ATTACH RULE:
  Brain §3 Always first (PROJECT_BRAIN + PROMPT_SCOPE). Then this setup/match pack
  (same order as CHAT_PROMPTS full START). Do not reshuffle; do not invent a new order.

---

## Context — what BE tip already has (do NOT re-implement on BE)

Backend tip (`professor-dock`) includes (vs older `main`):

1) **JWT Phase A** — POST /user/login, POST /user/refresh, Bearer; passport payload
   (access, refresh, user, adminCode, organizations[], supervisorOrgs[], profile).

2) **Org schema migrations** — Organization, SubAdminOrganization, Supervisor;
   organizationId on resources; adminCode kept. (Lab migrate **applied**; org **rows** may be 0.)

3) **Admin bootstrap** — GET /user/admin-bootstrap/ (ADMIN / SUPER_ADMIN / SUPERVISOR, Bearer).

4) **Return trip log** — ReturnTripLog + going/return board archives;
   DTODLOG return_* odometer fields moved onto ReturnTripLog;
   GET boarding_qr/?trip=return + scan via token; End archives RCList (no FE archive UI).

5) **SUPER_ADMIN** — userType for Django `/void/` web portal (is_staff + is_superuser);
   mobile shell = full ADMIN catalog.

6) **Settings / deps / gitignore** hygiene for JWT lab.

FE tip already merged: JWT + cream UI + role homes + SUPER_ADMIN shell + admin_bootstrap
+ return QR Phase 2 + SQLite schema v3 + Dart `returnTripLogId`/`tripLeg` + AGENTS.md.
Remotes keep-5 only.

Draft lag OK: prefer **API_CONTRACTS + code on tip** as wire truth; use drafts as shape hints.

---

## Goal (this chat)

Produce a **match report**, then **close FE gaps** against BE tip contracts/code.

### Work order (do not skip A→C)

  A. Confirm FE branch is professor-cts. BE path readable at professor-dock.
  B. **Code/contract match first** (read FE + docs + BE source). Docker up is optional.
  C. Write gap table: MATCH | FE_FIX | BE_FIX | DEFER (with file pointers).
  D. Implement **FE_FIX only** (Provider; module layout; no new REST unless required).
  E. After code edits: `flutter pub get`, `flutter analyze`, `flutter test`.
  F. Docs: API_CONTRACTS + touched feature README + DISCUSSION_STATUS if status changed.
  G. END sync: brain §5/§9, PROMPT_SCOPE, DOC_REGISTRY, CHAT_PROMPTS MIDDLE CONTEXT.

### Lab / migrate (optional — do not block the report)

- If Docker is already up on professor-dock: note whether migrations applied; optional
  light login/bootstrap probe.
- If Docker is down or migrate not done: still complete A–C and FE_FIX from contracts.
  Put "needs lab migrate" under DEFER / BE_FIX — do not stall the chat.

---

## Checklist — FE must match

### Auth / login JSON
[ ] ApiUrl + Dio: Bearer on non-login/refresh; refresh-on-401 once then /signIn
[ ] Login parse keys exactly per LOGIN_JSON_FIELDS.txt (camelCase wire only)
[ ] userType homes: ADMIN / SUPER_ADMIN / SUPERVISOR / DRIVER / COMMUTER / STAFF
    (STAFF → commuter UX; SUPERVISOR → admin shell + AdminService allow-list;
     SUPER_ADMIN → full shell; web org ops → Django /void/)
[ ] Empty organizations[] / supervisorOrgs[] / stub profile still valid (Phase A)

### Admin bootstrap → SQLite
[ ] After ADMIN / SUPER_ADMIN / SUPERVISOR JWT login, GET /user/admin-bootstrap/ soft-runs
[ ] Response shape vs ADMIN_BOOTSTRAP_DRAFT.md + API_CONTRACTS § Auth — Admin bootstrap
[ ] camelCase → snake_case SQLite schema **v3** (SQLITE_TABLES_COLUMNS.txt)
[ ] No UI screens required for bootstrap; Provider/repository only
[ ] 403 path for non-admin roles is safe (no crash)
[ ] Existing `lib/data/local/` SQLite DAO path is OK (legacy local DB) — do not
    invent presentation/domain folders; do not force a relocate this pass

### Org schema awareness (Phase A vs B)
[ ] FE tolerates null/missing routeCode, acType, area, organizations[]
[ ] Do **not** require org rows for login/bootstrap until Phase B fill
[ ] If BE already returns org fields after migrate: map into SQLite organization /
    sub_admin_organization / supervisor tables without inventing wire keys

### Return QR / return trip log
[ ] ApiUrl.boardingQr(batchId, trip: 'return') → ?trip=return
[ ] Mint: wire `return_trip_id` → Dart `returnTripLogId`; wire `trip` → Dart `tripLeg`
[ ] Scan/odometer store `returnTripLogId` + `tripLeg` for later consumers
[ ] Scan uses same boarding_scan; leg is in token (no extra FE body field)
[ ] Return scan: `allowJoinWaiting: false` (waiting = return_batch/add_commuter)
[ ] End trip: no FE archive UI; archives are BE-only
[ ] Odometer return leg uses ReturnTripLog fields (not old DTODLOG return_*)
[ ] Contracts: RETURN_TRIP_API_GAP.md · RETURN_QR_UI_PREP.md · batches README

### Morning D2D (regression — do not break)
[ ] Morning boarding_qr / boarding_scan / waiting join_waiting still work
[ ] WS ended-trip guard + isTripEnded still honored

### Routing / roles
[ ] ROUTING_AND_AUTH.md homes + AdminService allow-list still match code
[ ] Return boarding QR/scan routes gated correctly by role
[ ] isAdminLike includes ADMIN | SUPER_ADMIN | SUPERVISOR

---

## Out of scope (unless user says go)
- Client pack STEP 8 full device smoke (needs explicit **go**)
- VPS / merge BE tip → main
- BE code changes / running migrate as a required gate
- Senior JWT hardening
- Re-opening return-trip product decisions
- Navy UI redesign (cream is tip)
- New .md/.txt except updates to owners already listed
- FF/push of gb-f&d, p&gb-merger, gb-dock, p-gb-merger unless asked

---

## Non-negotiables
- State: Provider only (no Riverpod)
- Layout: features/.../screens|providers|models|repositories
  (no new presentation/domain). Existing lib/data/local for SQLite is allowed.
- Git: commit on professor-cts; never day-push main
- Prefer extend existing APIs; do not invent camelCase beyond contracts
- After FE code changes: flutter analyze + flutter test
- **Push policy:** docs-only → push professor-cts OK.
  Code FE_FIX → push professor-cts after analyze/test green.
  Full device pre-push gate (TESTING.md) only when user says **go** / pre-push —
  do not block the match report waiting for devices.

---

## Deliverables
1. Gap table in chat (MATCH | FE_FIX | BE_FIX | DEFER) with paths
2. FE_FIX commits on professor-cts (push day lane per push policy above)
3. Doc headers + API_CONTRACTS / feature README touch-ups for real deltas
4. Update CHAT_PROMPTS MIDDLE CONTEXT
5. Pasteable next START (device smoke or STEP 8 on go)

================================================================================
END COPY
================================================================================

After paste: fill PROMPT_SCOPE §1 for this chat; keep §2 — Q-fe-match-be **done**.
