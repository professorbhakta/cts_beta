CTS SCHEMA — FINAL DRAFT (saved)
================================
Updated: 2026-09-06
Status: FINAL DRAFT SAVED — no code/migrations until "green flag go"
Target path when PC online: D:\cts_beta\docs\setup\SCHEMA_FINAL_DRAFT.txt

------------------------------------------------------------------------
LOCKED DECISIONS
------------------------------------------------------------------------
- userType string + backend enums; API exposes allowed values; no UserType table
- userType: ADMIN | SUPERVISOR | DRIVER | COMMUTER | STAFF
- No personType on Commuter (rider kind = User.userType COMMUTER or STAFF)
- Fees on Commuter; hasPaid on User (often null initially)
- Import keys: externalPersonId AND registrationNo on Commuter
- DTODLOG extras: cabId, driverUserId, startedBy
- Organization table; KEEP SubAdminOrganization SEPARATE (do not merge into subAdmin)
- Keep adminCode AND add organizationId (adminCode = all-my-data; orgId = per-org)
- Cab regNumber globally unique; organizationId for group-by only
- PoP lat/longitude nullable (needed later when GPS collected)
- Thin Supervisor table (userId + organizationId)
- isComing today-only on Commuter
- SUPERVISOR allow-list: Batch, Cab, Route, PoP, day-ops (DTODLOG), Commuter
- Soft isActive / exit fields as listed
- Enums also: acType AC|NON_AC; exitKind LEFT|CANCELLED|null; startedBy ADMIN|SUPERVISOR|DRIVER; regStatus Active(+)

------------------------------------------------------------------------
1) User
------------------------------------------------------------------------
username, mobileNumber, email, address, userType, hasPaid, deviceId
(+ password, is_active, AbstractUser)

------------------------------------------------------------------------
2) Organization (NEW)
------------------------------------------------------------------------
id, orgName, isActive, createdAt, updatedAt

------------------------------------------------------------------------
3) subAdmin
------------------------------------------------------------------------
id, userId

------------------------------------------------------------------------
4) SubAdminOrganization (NEW join — KEEP SEPARATE)
------------------------------------------------------------------------
id, subAdminId, organizationId, isActive
unique (subAdminId, organizationId)

------------------------------------------------------------------------
5) Supervisor (thin)
------------------------------------------------------------------------
id, userId, organizationId, isActive
unique (userId, organizationId)

------------------------------------------------------------------------
6) Commuter
------------------------------------------------------------------------
userId, adminCode, organizationId, batchId, popId, cabId, isComing
externalPersonId, registrationNo, gender, registrationDate, regStatus, referenceBy
academicYear, institute, course, department, program, enrollmentNo, currentSemOrYear, erpRollNo
designation, employeeType, joiningDate, staffErpId
altEmail, otherContact, city, pincode, bloodGroup
busPassNo, validFrom, validTo, isFreeOfCharge, busPassIssuedAt
exitAt, exitReason, exitKind
feeAmount, scholarship, feePaid, outstanding
isActive, createdAt, updatedAt

------------------------------------------------------------------------
7) Routes
------------------------------------------------------------------------
routeName, adminCode, organizationId, routeCode, isActive

------------------------------------------------------------------------
8) pickUpPoints
------------------------------------------------------------------------
pickUpPointName, lat (nullable), longitude (nullable), routeId, adminCode, organizationId, inLine, area, isActive

------------------------------------------------------------------------
9) Batch
------------------------------------------------------------------------
batchName, batchTime, end_time, startDate, endDate, adminCode, organizationId, isActive

------------------------------------------------------------------------
10) cab
------------------------------------------------------------------------
regNumber (globally unique), capacity, km, regDate, routeId, thumbnail, trackingVehicleId
adminCode, organizationId, acType, isActive

------------------------------------------------------------------------
11) Driver
------------------------------------------------------------------------
userId, adminCode, organizationId, batchId, cabId, isActive

------------------------------------------------------------------------
12) DTODLOG
------------------------------------------------------------------------
batchId, tripDate, CList, isActive
startTime, endTime, return_start_time, return_end_time
morning_start_km, morning_end_km, morning_start_photo, morning_end_photo
morning_start_recorded_at, morning_end_recorded_at
return_start_km, return_end_km, return_start_photo, return_end_photo
return_start_recorded_at, return_end_recorded_at
cabId, driverUserId, startedBy, organizationId, adminCode
unique (batchId, tripDate)

------------------------------------------------------------------------
RETURN TRIP SPLIT (draft 2026-09-08) — see RETURN_TRIP_COLUMNS_DRAFT.md
------------------------------------------------------------------------
New: ReturnTripLog (return start/end/km/photo + RCList mirror + boardArchiveId)
New: ReturnBoardArchive (userIds snapshot on End)
Later: strip return_* from morning DTODLOG
Live boarded IDs: Redis + sockets (not HTTPS polling)